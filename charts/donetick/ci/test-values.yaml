ingress:
  enabled: true
  hosts:
    - host: donetick.example.local
      paths:
        - path: /
          pathType: Prefix

postgresql:
  enabled: false

database:
  sqlite:
    persistence:
      enabled: true

storage:
  mode: local
  local:
    persistence:
      enabled: true
