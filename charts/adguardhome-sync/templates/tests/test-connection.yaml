apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adguardhome-sync.fullname" . }}-test-connection"
  labels:
    {{- include "adguardhome-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-O-', '{{ include "adguardhome-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
